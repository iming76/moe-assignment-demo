{
  "name": "moe-assignment-demo",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "check-types": "pnpm -r run check-types"
  }
}
