{
  "name": "@handwriting-extraction-demo/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uv run uvicorn app.api:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step required for Python backend'",
    "lint": "echo 'no linter configured'",
    "check-types": "mypy app --ignore-missing-imports",
    "test": "echo 'no tests configured'"
  }
}