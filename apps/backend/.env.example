OPENAI_API_KEY=sk-your-project-key
# Only needed if vision_llm.provider is switched to the corresponding provider
# in app/config.yaml — see the commented examples there.
# OPENROUTER_API_KEY=sk-or-your-key
